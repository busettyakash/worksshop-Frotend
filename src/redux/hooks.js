import { useDispatch, useSelector } from 'react-redux'

// Typed hooks (JavaScript version — just re-exports)
export const useAppDispatch = () => useDispatch()
export const useAppSelector = useSelector
